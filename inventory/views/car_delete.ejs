<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Delete Car</title>
        <link rel="stylesheet" href="/stylesheets/cardelete.modules.css" />
    </head>
    <body>
        <div class="container">
            <h1 class="car-name"><%= car.name %></h1>
            <img
                src="<%= car.imgUrl %>"
                alt="<%= car.name %>"
                class="car-image"
            />
            <form action="/cars/<%= car._id %>/delete" method="POST">
                <button type="submit" class="delete-button">
                    Permanently Delete
                </button>
            </form>
        </div>
    </body>
</html>
